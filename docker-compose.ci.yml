version: '2'
services:
  web:
    image: ${IMAGE_NAME}
    volumes:
       - "web-data:/app"
    ports:
      - "3000"
    networks:
       - ci
    labels:
      - "traefik.backend=design-prototypes"
      - "traefik.frontend.rule=Host:design-prototypes.ci.ukpds.org"

networks:
  default:
    external:
      name: ci

volumes:
    web-data:
      driver: local